<div data-turbo="false" class="mx-auto max-w-7xl space-y-6 pb-12">

  <!-- FLASH MESSAGES (anti doublon) -->
  <% notice = flash[:notice].presence %>
  <% alert  = flash[:alert].presence %>

  <% if notice || alert %>
    <div class="space-y-3">
      <% if notice %>
        <div class="rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800 shadow-sm">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="rounded-2xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm font-medium text-rose-800 shadow-sm">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <%# Empêche la ré-affichage du flash (souvent cause des doublons) %>
    <% flash.discard %>
  <% end %>

  <!-- ================================================= -->
  <!-- HEADER -->
  <!-- ================================================= -->
  <section class="rounded-[32px] bg-white ring-1 ring-slate-200 shadow-[0_18px_70px_rgba(15,23,42,0.10)] overflow-hidden">
    <div class="p-6 md:p-8">
      <div class="flex flex-col gap-6 lg:flex-row lg:items-start lg:justify-between">

        <div class="min-w-0">
          <div class="flex flex-wrap items-center gap-3">
            <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-[11px] font-semibold uppercase tracking-wide text-slate-700 ring-1 ring-slate-200">
              <span class="h-1.5 w-1.5 rounded-full bg-violet-600"></span>
              Mes2Vis · Abonnements
            </span>

            <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 text-[11px] font-semibold text-slate-700 ring-1 ring-slate-200">
              <span class="h-1.5 w-1.5 rounded-full bg-slate-400"></span>
              Paiement sécurisé Stripe
            </span>
          </div>

          <h1 class="mt-5 text-2xl md:text-3xl font-semibold tracking-tight text-slate-900">
            Choisis ton abonnement
          </h1>

          <p class="mt-1 text-sm text-slate-600 max-w-2xl">
            Débloque l’accès complet à Mes2Vis. Paiement et gestion d’abonnement via Stripe.
          </p>
        </div>

        <!-- Statut -->
        <div class="w-full max-w-lg">
          <% if @user.subscribed? %>
            <div class="rounded-3xl bg-white ring-1 ring-slate-200 shadow-sm overflow-hidden">
              <div class="h-1 w-full bg-gradient-to-r from-indigo-500 via-violet-600 to-fuchsia-500"></div>

              <div class="p-5">
                <p class="text-[10px] font-semibold uppercase tracking-wide text-slate-500">
                  Ton offre actuelle
                </p>

                <p class="mt-2 text-lg font-semibold text-slate-900">
                  <% if @user.early_access? %>
                    Early Access – 3,99 €/mois
                  <% else %>
                    Standard – 8,99 €/mois
                  <% end %>
                </p>

                <div class="mt-3 grid gap-2 text-sm text-slate-700">
                  <p>
                    <span class="text-slate-500">Statut :</span>
                    <span class="font-semibold text-slate-900"><%= @user.subscription_status %></span>
                  </p>

                  <% if @user.subscription_current_period_end %>
                    <p>
                      <span class="text-slate-500">Renouvellement :</span>
                      <span class="font-semibold text-slate-900"><%= @user.subscription_current_period_end.strftime("%d/%m/%Y") %></span>
                    </p>
                  <% end %>
                </div>

                <div class="mt-4">
                  <%= button_to "Gérer mon abonnement",
                                billing_portal_subscription_path,
                                method: :get,
                                class: "w-full inline-flex items-center justify-center rounded-2xl px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition active:scale-[0.99]
                                        bg-gradient-to-r from-indigo-500 via-violet-600 to-fuchsia-500 hover:brightness-110" %>
                </div>

                <p class="mt-2 text-[11px] text-slate-400">
                  Tu peux changer d’offre ou résilier depuis le portail Stripe.
                </p>
              </div>
            </div>
          <% else %>
            <div class="rounded-3xl bg-white ring-1 ring-slate-200 shadow-sm overflow-hidden">
              <div class="h-1 w-full bg-slate-900"></div>

              <div class="p-5">
                <p class="text-[10px] font-semibold uppercase tracking-wide text-slate-500">
                  Statut
                </p>
                <p class="mt-2 text-lg font-semibold text-slate-900">
                  Aucun abonnement actif
                </p>
                <p class="mt-1 text-sm text-slate-600">
                  Choisis une offre ci-dessous pour débloquer toutes les fonctionnalités.
                </p>
              </div>
            </div>
          <% end %>
        </div>

      </div>
    </div>
  </section>

  <!-- ================================================= -->
  <!-- CARTES TARIFS -->
  <!-- ================================================= -->
  <section class="grid gap-6 md:grid-cols-2">

    <!-- EARLY ACCESS -->
    <div class="relative rounded-3xl bg-white ring-1 ring-slate-200 shadow-[0_18px_70px_rgba(15,23,42,0.08)] overflow-hidden flex flex-col">
      <div class="h-1 w-full bg-gradient-to-r from-indigo-500 via-violet-600 to-fuchsia-500"></div>

      <!-- Badge -->
      <div class="p-6 pb-0">
        <span class="inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-indigo-50 via-violet-50 to-fuchsia-50 px-3 py-1 text-xs font-semibold text-violet-700 ring-1 ring-slate-200">
          <span class="h-1.5 w-1.5 rounded-full bg-violet-600"></span>
          Early · 200 premiers
        </span>
      </div>

      <div class="p-6 pt-5 flex-1 flex flex-col">
        <!-- Prix -->
        <div>
          <p class="text-[11px] font-semibold uppercase tracking-wide text-slate-500">Early Access</p>

          <div class="mt-2 flex items-baseline gap-2">
            <span class="text-4xl font-semibold tracking-tight text-slate-900">3,99€</span>
            <span class="text-sm text-slate-500">/ mois</span>
          </div>

          <p class="mt-2 text-sm font-medium text-violet-700">
            Prix garanti à vie pour les 200 premiers utilisateurs.
          </p>
        </div>

        <!-- Features -->
        <ul class="mt-5 space-y-2 text-sm text-slate-700">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-violet-50 text-violet-700 ring-1 ring-violet-100">✓</span>
            Accès complet à Mes2Vis
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-violet-50 text-violet-700 ring-1 ring-violet-100">✓</span>
            Mises à jour incluses
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-violet-50 text-violet-700 ring-1 ring-violet-100">✓</span>
            Prioritaire sur les nouvelles fonctionnalités
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-violet-50 text-violet-700 ring-1 ring-violet-100">✓</span>
            Aucune hausse de prix
          </li>
        </ul>

        <!-- CTA -->
        <div class="mt-6">
          <% if @can_early %>
            <%= button_to "Choisir cette offre",
                          create_checkout_session_subscription_path(plan: "early"),
                          method: :post,
                          class: "w-full inline-flex items-center justify-center rounded-2xl px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition active:scale-[0.99]
                                  bg-gradient-to-r from-indigo-500 via-violet-600 to-fuchsia-500 hover:brightness-110 cursor-pointer" %>

            <p class="mt-2 text-[11px] text-slate-400 text-center">
              Places restantes : <%= 200 - @early_count %> (approx.)
            </p>
          <% else %>
            <button class="w-full rounded-2xl bg-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-500 cursor-not-allowed ring-1 ring-slate-200">
              Offre complète
            </button>
            <p class="mt-2 text-[11px] text-slate-400 text-center">
              L’offre early n’est plus disponible.
            </p>
          <% end %>
        </div>

        <!-- Bottom note -->
        <div class="mt-5 rounded-2xl bg-slate-50 ring-1 ring-slate-200 p-4">
          <p class="text-[11px] font-semibold uppercase tracking-wide text-slate-500">Idéal si…</p>
          <p class="mt-1 text-sm text-slate-700">
            tu veux le meilleur prix à vie + être prioritaire sur les nouveautés.
          </p>
        </div>
      </div>
    </div>

    <!-- STANDARD -->
    <div class="relative rounded-3xl bg-white ring-1 ring-slate-200 shadow-[0_18px_70px_rgba(15,23,42,0.08)] overflow-hidden flex flex-col">
      <div class="h-1 w-full bg-slate-900"></div>

      <div class="p-6 pb-0">
        <span class="inline-flex items-center gap-2 rounded-full bg-slate-900 px-3 py-1 text-xs font-semibold text-white shadow-sm">
          Standard
        </span>
      </div>

      <div class="p-6 pt-5 flex-1 flex flex-col">
        <!-- Prix -->
        <div>
          <p class="text-[11px] font-semibold uppercase tracking-wide text-slate-500">Standard</p>

          <div class="mt-2 flex items-baseline gap-2">
            <span class="text-4xl font-semibold tracking-tight text-slate-900">8,99€</span>
            <span class="text-sm text-slate-500">/ mois</span>
          </div>

          <p class="mt-2 text-sm text-slate-600">
            Sans engagement. Tu peux gérer ton abonnement à tout moment.
          </p>
        </div>

        <!-- Features -->
        <ul class="mt-5 space-y-2 text-sm text-slate-700">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-slate-700 ring-1 ring-slate-200">✓</span>
            Accès complet à Mes2Vis
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-slate-700 ring-1 ring-slate-200">✓</span>
            Mises à jour incluses
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-slate-700 ring-1 ring-slate-200">✓</span>
            Support par email
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-slate-700 ring-1 ring-slate-200">✓</span>
            Idéal auto-entrepreneurs & petites équipes
          </li>
        </ul>

        <!-- CTA -->
        <div class="mt-6">
          <%= button_to "Choisir cette offre",
                        create_checkout_session_subscription_path(plan: "standard"),
                        method: :post,
                        class: "w-full inline-flex items-center justify-center rounded-2xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-slate-800 transition cursor-pointer" %>
        </div>

        <div class="mt-5 rounded-2xl bg-slate-50 ring-1 ring-slate-200 p-4">
          <p class="text-[11px] font-semibold uppercase tracking-wide text-slate-500">Idéal si…</p>
          <p class="mt-1 text-sm text-slate-700">
            tu veux une offre simple et stable, sans limite, et gérable à tout moment.
          </p>
        </div>
      </div>
    </div>

  </section>

  <p class="text-[11px] text-slate-400">
    Paiements sécurisés via Stripe. Tu peux gérer ou résilier ton abonnement à tout moment depuis le portail.
  </p>

</div>
;
